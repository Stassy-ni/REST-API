paths:
  /communication/conversations:
    get:
      summary: List conversations
      description: Get conversations where user is a participant.
      tags:
        - Communication
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Conversations
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '../../components/schemas/communication/Conversation.yaml'
